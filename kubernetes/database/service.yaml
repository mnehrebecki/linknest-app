apiVersion: v1
kind: Service
metadata:
  name: database-svc
  namespace: linknest-app
  labels:
    app: linknest
    role: database
spec:
  ports:
  - port: 5432
    name: database
  clusterIP: None
  selector:
    role: database